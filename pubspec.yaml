name: vet_mobile_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  go_router: ^15.1.2
  flutter_svg: ^2.0.9
  cupertino_icons: ^1.0.2
  http: ^1.3.0
  
  # Firebase dependencies
  firebase_auth: ^5.1.0
  firebase_core: ^3.13.0
  google_sign_in: ^6.2.1
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/icons/menu/
    - assets/icons/common/
    - assets/images/
    - assets/icons/common/logo.svg
    - assets/icons/common/google_button.svg
    - assets/images/grass_banner.svg
    - assets/images/feed_topic1.svg
    - assets/images/feed_topic2.svg
    - assets/images/feed_topic3.svg