import 'package:flutter/material.dart';

class VetInfo extends StatelessWidget {
  const VetInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Vet Info')),
      body: const Center(child: Text('Vet Info Screen')),
    );
  }
}
